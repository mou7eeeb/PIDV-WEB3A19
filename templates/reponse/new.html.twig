{% extends 'base.html.twig' %}

{% block title %}Ajouter une Réponse{% endblock %}

{% block body %}
    <h1>💬 Nouvelle Réponse</h1>

    {{ include('reponse/_form.html.twig', {'form': form, 'button_label': '💾 Envoyer la réponse'}) }}

    <a href="{{ path('app_reclamation_index') }}">↩️ Retour à la liste des réclamations</a>
{% endblock %}

{% block javascripts %}
<script>
document.addEventListener('DOMContentLoaded', function () {
    const form = document.getElementById('reponse-form');
    if (!form) return;

    form.addEventListener('submit', function (e) {
        const contenu = form.querySelector('[name="reponse[contenu]"]');
        const errors = [];

        if (!contenu.value.trim() || contenu.value.trim().length < 10) {
            errors.push("Le contenu de la réponse doit contenir au moins 10 caractères.");
        }

        if (errors.length > 0) {
            e.preventDefault();
            alert(errors.join("\n"));
        }
    });
});
</script>
{% endblock %}
