{% extends 'base.html.twig' %}

{% block title %}Modifier une réponse{% endblock %}

{% block body %}
    <h1>✏️ Modifier la réponse</h1>

    {{ include('reponse/_form.html.twig', {'form': form, 'button_label': '💾 Mettre à jour la réponse'}) }}

    <a href="{{ path('app_reponse_index') }}">↩️ Retour à la liste des réponses</a>
{% endblock %}

{% block javascripts %}
<script>
document.addEventListener('DOMContentLoaded', function () {
    const form = document.getElementById('reponse-form');
    if (!form) return;

    form.addEventListener('submit', function (e) {
        const contenu = form.querySelector('[name="reponse[contenu]"]');
        const errors = [];

        // Validation du champ contenu (doit contenir au moins 10 caractères)
        if (!contenu.value.trim() || contenu.value.trim().length < 10) {
            errors.push("Le contenu de la réponse doit contenir au moins 10 caractères.");
        }

        // Si des erreurs sont trouvées, empêcher l'envoi du formulaire et afficher les erreurs
        if (errors.length > 0) {
            e.preventDefault();
            alert(errors.join("\n"));
        }
    });
});
</script>
{% endblock %}
