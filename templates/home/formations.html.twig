{% extends 'base.html.twig' %}

{% block title %}Toutes les formations disponibles{% endblock %}

{% block stylesheets %}
<style>
    :root {
        --primary-color: #4f86f7;
        --secondary-color: #3a75e0;
        --accent-color: #6e9fff;
        --dark-bg: rgba(67, 90, 111, 0.95);
        --light-text: #ffffff;
        --border-radius: 8px;
        --box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -2px rgba(0, 0, 0, 0.05);
    }
    
    .page-header {
        background-color: var(--dark-bg);
        color: var(--light-text);
        padding: 3rem 0;
        margin-bottom: 3rem;
        text-align: center;
    }
    
    .page-header h1 {
        font-weight: 700;
        margin-bottom: 1rem;
    }
    
    .page-header p {
        font-size: 1.1rem;
        max-width: 700px;
        margin: 0 auto;
        opacity: 0.9;
    }
    
    .formation-card {
        border-radius: var(--border-radius);
        overflow: hidden;
        box-shadow: var(--box-shadow);
        transition: transform 0.3s, box-shadow 0.3s;
        margin-bottom: 2rem;
        border: none;
        height: 100%;
        background-color: white;
    }
    
    .formation-card:hover {
        transform: translateY(-5px);
        box-shadow: 0 20px 25px -5px rgba(0, 0, 0, 0.1), 0 10px 10px -5px rgba(0, 0, 0, 0.04);
    }
    
    .formation-card .card-header {
        background-color: var(--primary-color);
        color: var(--light-text);
        font-weight: 600;
        padding: 1rem;
        border: none;
    }
    
    .formation-card .card-body {
        padding: 1.5rem;
    }
    
    .formation-card .card-title {
        font-weight: 700;
        margin-bottom: 1rem;
        color: #2d3748;
    }
    
    .formation-info {
        display: flex;
        justify-content: space-between;
        margin-bottom: 1rem;
        color: #4a5568;
        font-size: 0.9rem;
    }
    
    .formation-info span {
        display: flex;
        align-items: center;
    }
    
    .formation-info i {
        margin-right: 0.5rem;
        color: var(--primary-color);
    }
    
    .formation-price {
        font-size: 1.5rem;
        font-weight: 700;
        color: var(--primary-color);
        margin-bottom: 1.5rem;
    }
    
    .btn-primary {
        background-color: var(--primary-color);
        border-color: var(--primary-color);
        padding: 0.5rem 1.5rem;
        font-weight: 600;
    }
    
    .btn-primary:hover {
        background-color: var(--secondary-color);
        border-color: var(--secondary-color);
        transform: translateY(-2px);
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
    }
    
    .filters {
        background-color: #f8fafc;
        border-radius: var(--border-radius);
        padding: 1.5rem;
        margin-bottom: 2rem;
        box-shadow: var(--box-shadow);
    }
    
    .filters h3 {
        font-size: 1.25rem;
        font-weight: 600;
        margin-bottom: 1.5rem;
        color: #2d3748;
    }
    
    .filter-group {
        margin-bottom: 1.5rem;
    }
    
    .filter-group label {
        font-weight: 600;
        margin-bottom: 0.5rem;
        color: #4a5568;
    }
    
    .filter-group .form-control {
        border-radius: var(--border-radius);
    }
    
    .no-formations {
        text-align: center;
        padding: 3rem;
        background-color: #f8fafc;
        border-radius: var(--border-radius);
        margin: 2rem 0;
    }
    
    .no-formations h3 {
        font-weight: 600;
        margin-bottom: 1rem;
        color: #2d3748;
    }
    
    .no-formations p {
        color: #4a5568;
        margin-bottom: 1.5rem;
    }
</style>
{% endblock %}

{% block body %}
<!-- Header -->
<header class="page-header">
    <div class="container">
        <h1>
            {% if category is defined %}
                Formations en {{ category }}
            {% else %}
                Toutes les formations disponibles
            {% endif %}
        </h1>
        <p>Découvrez nos formations professionnelles pour développer vos compétences et booster votre carrière</p>
    </div>
</header>

<div class="container">
    <div class="row">
        <!-- Filtres -->
        <div class="col-lg-3">
            <div class="filters">
                <h3>Filtres</h3>
                <div class="filter-group">
                    <label for="category">Catégorie</label>
                    <select class="form-select" id="category">
                        <option value="">Toutes les catégories</option>
                        <option value="Développement Web" {% if category is defined and category == 'Développement Web' %}selected{% endif %}>Développement Web</option>
                        <option value="Design & Création" {% if category is defined and category == 'Design & Création' %}selected{% endif %}>Design & Création</option>
                        <option value="Marketing Digital" {% if category is defined and category == 'Marketing Digital' %}selected{% endif %}>Marketing Digital</option>
                        <option value="Rédaction & Traduction" {% if category is defined and category == 'Rédaction & Traduction' %}selected{% endif %}>Rédaction & Traduction</option>
                    </select>
                </div>
                <div class="filter-group">
                    <label for="price">Prix maximum</label>
                    <input type="range" class="form-range" id="price" min="0" max="10000" step="100" value="10000">
                    <div class="d-flex justify-content-between">
                        <span>0 €</span>
                        <span id="priceValue">10 000 €</span>
                    </div>
                </div>
                <div class="filter-group">
                    <label for="duration">Durée maximum (heures)</label>
                    <input type="range" class="form-range" id="duration" min="1" max="240" step="1" value="240">
                    <div class="d-flex justify-content-between">
                        <span>1h</span>
                        <span id="durationValue">240h</span>
                    </div>
                </div>
                <button class="btn btn-primary w-100">Appliquer les filtres</button>
            </div>
        </div>
        
        <!-- Liste des formations -->
        <div class="col-lg-9">
            {% if formations|length > 0 %}
                <div class="row">
                    {% for formation in formations %}
                        <div class="col-md-6 col-lg-4 mb-4">
                            <div class="formation-card">
                                <div class="card-header">
                                    {{ formation.publicationId }}
                                </div>
                                <div class="card-body">
                                    <h5 class="card-title">Formation {{ formation.publicationId }}</h5>
                                    <div class="formation-info">
                                        <span><i class="bi bi-clock"></i> {{ formation.duree }}h</span>
                                        <span><i class="bi bi-people"></i> {{ formation.nombrePlaces }} places</span>
                                    </div>
                                    <div class="formation-price">{{ formation.prix }} €</div>
                                    <a href="#" class="btn btn-primary w-100">S'inscrire</a>
                                </div>
                            </div>
                        </div>
                    {% endfor %}
                </div>
            {% else %}
                <div class="no-formations">
                    <h3>Aucune formation disponible</h3>
                    <p>Nous n'avons pas encore de formations dans cette catégorie. Revenez bientôt pour découvrir nos nouvelles offres.</p>
                    <a href="{{ path('app_home') }}" class="btn btn-primary">Retour à l'accueil</a>
                </div>
            {% endif %}
        </div>
    </div>
</div>

<script>
    document.addEventListener('DOMContentLoaded', function() {
        // Mise à jour des valeurs des filtres
        const priceRange = document.getElementById('price');
        const priceValue = document.getElementById('priceValue');
        const durationRange = document.getElementById('duration');
        const durationValue = document.getElementById('durationValue');
        
        priceRange.addEventListener('input', function() {
            priceValue.textContent = this.value + ' €';
        });
        
        durationRange.addEventListener('input', function() {
            durationValue.textContent = this.value + 'h';
        });
        
        // Redirection lors du changement de catégorie
        const categorySelect = document.getElementById('category');
        categorySelect.addEventListener('change', function() {
            if (this.value) {
                window.location.href = '{{ path('app_formations_category', {'category': 'CATEGORY'}) }}'.replace('CATEGORY', this.value);
            } else {
                window.location.href = '{{ path('app_formations') }}';
            }
        });
    });
</script>
{% endblock %}
