<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Détails de l'inscription #{{ inscription.id }}</title>
    <style>
        body {
            font-family: Helvetica, Arial, sans-serif;
            margin: 0;
            padding: 20px;
            color: #333;
        }
        .header {
            text-align: center;
            margin-bottom: 30px;
            border-bottom: 1px solid #ddd;
            padding-bottom: 10px;
        }
        .header h1 {
            color: #15222b;
            margin: 0;
            padding: 0;
            font-size: 24px;
        }
        .header p {
            color: #666;
            margin: 5px 0 0;
            font-size: 14px;
        }
        .card {
            border: 1px solid #ddd;
            border-radius: 8px;
            padding: 20px;
            margin-bottom: 20px;
            background-color: #fff;
        }
        .card-header {
            border-bottom: 1px solid #eee;
            padding-bottom: 15px;
            margin-bottom: 15px;
        }
        .card-title {
            margin: 0;
            color: #15222b;
            font-size: 18px;
        }
        .info-row {
            margin-bottom: 15px;
            display: block;
        }
        .info-label {
            font-weight: bold;
            display: inline-block;
            width: 150px;
            color: #555;
        }
        .info-value {
            display: inline-block;
        }
        .badge {
            display: inline-block;
            padding: 3px 7px;
            font-size: 12px;
            border-radius: 4px;
            background-color: #e9ecef;
        }
        .badge-primary {
            background-color: #cfe2ff;
            color: #0d6efd;
        }
        .badge-info {
            background-color: #cff4fc;
            color: #0dcaf0;
        }
        .footer {
            margin-top: 30px;
            text-align: center;
            font-size: 12px;
            color: #666;
            border-top: 1px solid #ddd;
            padding-top: 10px;
        }
    </style>
</head>
<body>
    <div class="header">
        <h1>Détails de l'inscription #{{ inscription.id }}</h1>
        <p>Généré le {{ "now"|date("d/m/Y à H:i") }}</p>
    </div>
    
    <div class="card">
        <div class="card-header">
            <h2 class="card-title">Informations d'inscription</h2>
        </div>
        
        <div class="info-row">
            <span class="info-label">ID d'inscription:</span>
            <span class="info-value">{{ inscription.id }}</span>
        </div>
        
        <div class="info-row">
            <span class="info-label">ID Utilisateur:</span>
            <span class="info-value">
                <span class="badge badge-primary">{{ inscription.userId }}</span>
            </span>
        </div>
        
        <div class="info-row">
            <span class="info-label">ID Formation:</span>
            <span class="info-value">
                <span class="badge badge-info">{{ inscription.formationId }}</span>
            </span>
        </div>
        
        <div class="info-row">
            <span class="info-label">Date d'inscription:</span>
            <span class="info-value">{{ inscription.dateInscription ? inscription.dateInscription|date('d/m/Y') : 'Non spécifiée' }}</span>
        </div>
    </div>
    
    <div class="footer">
        <p>Document généré automatiquement - {{ "now"|date("d/m/Y") }}</p>
    </div>
</body>
</html>
